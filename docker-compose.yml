version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      # Persistent uploads
      - uploads_data:/app/public/uploads
    environment:
      - NODE_ENV=production

volumes:
  uploads_data:
    driver: local
